{
  "name": "article-rewriter",
  "version": "1.0.0",
  "description": "Article rewriter plugin for WordPress",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start --webpack-src-dir=src",
    "check-engines": "wp-scripts check-engines"
  },
  "devDependencies": {
    "@wordpress/scripts": "^26.1.0"
  }
}